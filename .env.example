# ===========================================
# VARIABLES D'ENVIRONNEMENT POUR RAILWAY
# ===========================================
# Copiez ce fichier et renommez-le en .env pour le développement local
# Sur Railway, configurez ces variables dans le dashboard

# Base de données PostgreSQL
# Railway fournit automatiquement cette variable si vous ajoutez un service PostgreSQL
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# Configuration Next.js
NODE_ENV="production"

# URL de l'application (Railway la définit automatiquement via RAILWAY_PUBLIC_DOMAIN)
# NEXT_PUBLIC_APP_URL="https://votre-app.railway.app"

# ===========================================
# VARIABLES OPTIONNELLES
# ===========================================

# Authentification (si vous utilisez NextAuth.js)
# NEXTAUTH_SECRET="votre-secret-genere"
# NEXTAUTH_URL="https://votre-app.railway.app"

# APIs externes (ajoutez selon vos besoins)
# API_KEY="votre-cle-api"

# ===========================================
# SÉCURITÉ
# ===========================================

# Clé de chiffrement pour les tokens OAuth Google (32 bytes en hex = 64 caractères)
# Générer avec: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY="votre-cle-de-chiffrement-64-caracteres-hex"

# Google OAuth
GOOGLE_CLIENT_ID="votre-google-client-id"
GOOGLE_CLIENT_SECRET="votre-google-client-secret"
GOOGLE_REDIRECT_URI="https://votre-app.railway.app/api/auth/google/callback"

# OpenAI (pour Leo)
OPENAI_API_KEY="votre-openai-api-key"

# AWS S3 (pour les uploads)
AWS_REGION="ca-central-1"
AWS_ACCESS_KEY_ID="votre-aws-access-key"
AWS_SECRET_ACCESS_KEY="votre-aws-secret-key"
AWS_S3_BUCKET="nukleo-hub"
